# Kiro Client TUI 使用指南

## 🚀 快速开始

### 安装和运行

```bash
# 克隆仓库
git clone https://github.com/NevinXuHui/kiro-client.git
cd kiro-client

# 切换到 TUI 分支
git checkout tui-version

# 编译
go build -o kiro-tui ./cmd/tui/

# 运行
./kiro-tui
```

## 📺 界面导航

### 主菜单

```
🔧 Kiro Client - TUI

▶ 📝 注册机 - 批量注册 AWS Builder ID 账号
  📊 号池管理 - 查看和管理已注册的账号
  🌐 网关控制 - 启动/停止 Kiro 本地网关
  ⚙️ 设置 - 配置代理池、域名池等
  🚪 退出 - 退出程序

↑/↓: 导航  Enter: 选择  Ctrl+C/q: 退出
```

#### 操作说明
- **↑/↓ 或 k/j**: 上下移动光标
- **Enter 或 空格**: 选择菜单项
- **q 或 Ctrl+C**: 退出程序

## 📝 注册机界面

### 界面布局

```
┌─────────────────────────────────────────┐
│ 🔧 注册机                               │
├─────────────────────────────────────────┤
│                                         │
│ ┌────────────────────────────────────┐  │
│ │ 邮箱源 (←/→ 切换):                 │  │
│ │                                    │  │
│ │   [1] Outlook                      │  │
│ │ ▶ [2] CloudMail                    │  │
│ │   [3] HttpAPI                      │  │
│ └────────────────────────────────────┘  │
│                                         │
│   数量       [10          ]             │
│   并发       [3           ]             │
│   延时(秒)   [5           ]             │
│                                         │
│   [开始注册]                            │
│                                         │
├─────────────────────────────────────────┤
│ Tab: 切换  ←/→: 切换邮箱源              │
│ Enter: 开始/停止  ESC: 返回  q: 退出    │
└─────────────────────────────────────────┘
```

### 详细操作

#### 1. 选择邮箱源

**方式 A: 左右箭头键（推荐）**
```
← 或 h: 切换到上一个邮箱源
→ 或 l: 切换到下一个邮箱源

循环顺序:
Outlook ⇄ CloudMail ⇄ HttpAPI
```

**方式 B: 数字键**
```
1: 选择 Outlook
2: 选择 CloudMail
3: 选择 HttpAPI

注意: 仅在焦点不在输入框时生效
```

#### 2. 配置参数

**切换输入框**
```
Tab: 切换到下一个输入框
Shift+Tab: 切换到上一个输入框
↑/↓: 上下切换
```

**输入框说明**

| 参数 | 默认值 | 说明 | 建议范围 |
|------|--------|------|----------|
| 数量 | 10 | 注册账号数量 | 1-100 |
| 并发 | 3 | 同时运行的任务数 | 1-10 |
| 延时(秒) | 5 | 每个账号启动间隔 | 3-15 |

**配置示例**

测试场景:
```
数量: 2
并发: 1
延时: 0
```

少量注册:
```
数量: 5
并发: 2
延时: 5
```

批量注册:
```
数量: 20
并发: 5
延时: 10
```

#### 3. 开始注册

```
1. Tab 切换到 [开始注册] 按钮
2. 按 Enter 开始任务
3. 任务运行时按钮变为 [停止任务]
4. 再次按 Enter 可以停止任务
```

### 进度显示

任务开始后会显示实时进度：

```
┌────────────────────────────────────┐
│ 进度 (3/10)                        │
│                                    │
│ ████████████░░░░░░░░░░░░ 30.0%    │
│                                    │
│ ✓ 成功: 2  ✗ 失败: 1  ↻ 进行中: 7 │
│                                    │
│ 当前: user4@example.com            │
└────────────────────────────────────┘
```

**进度信息说明**
- **进度条**: 视觉展示完成百分比
- **成功数**: 绿色显示，成功注册的账号
- **失败数**: 红色显示，注册失败的账号
- **进行中**: 正在注册的账号数
- **当前任务**: 显示正在处理的邮箱

### 日志输出

```
┌────────────────────────────────────┐
│ 日志                               │
│                                    │
│ 21:27:15 • 启动任务: 10个账号...   │
│ 21:27:18 ✓ user1@... - 成功       │
│ 21:27:23 ✓ user2@... - 成功       │
│ 21:27:28 ✗ user3@... - 邮箱已注册 │
│ 21:27:33 ↻ user4@... - 正在注册... │
└────────────────────────────────────┘
```

**日志级别标识**
- **•** (info): 普通信息
- **✓** (success): 成功，绿色
- **✗** (error): 错误，红色
- **⚠** (warning): 警告，黄色
- **↻** (in-progress): 进行中

### 完整操作流程

#### 场景 1: 使用 CloudMail 注册 5 个账号

```
步骤 1: 启动 TUI
$ ./kiro-tui

步骤 2: 进入注册机
[主菜单] 光标在 "注册机" 上
按 Enter

步骤 3: 确认邮箱源
[注册机] 默认选中 CloudMail (▶ 标记)
如果需要切换，按 ← 或 →

步骤 4: 配置数量
[注册机] 按 Tab 切换到 "数量" 输入框
清空内容（Ctrl+U 或全选删除）
输入: 5

步骤 5: 配置并发
按 Tab 切换到 "并发" 输入框
输入: 2

步骤 6: 配置延时
按 Tab 切换到 "延时(秒)" 输入框
输入: 5

步骤 7: 开始注册
按 Tab 切换到 [开始注册] 按钮
按 Enter

步骤 8: 查看进度
观察进度条和日志输出
等待完成或按 Enter 停止

步骤 9: 返回主菜单
按 ESC
```

#### 场景 2: 切换到 Outlook 邮箱源

```
步骤 1: 在注册机界面
确保焦点不在输入框上
（如果在输入框，按 Tab 几次移开）

步骤 2: 切换邮箱源
按 ← 键两次
或直接按数字键 1

步骤 3: 确认切换
看到 ▶ 标记移动到 [1] Outlook
```

## 🎯 快捷键参考

### 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| **q** | 退出程序（主菜单）或返回主菜单（其他界面） |
| **Ctrl+C** | 强制退出程序 |
| **ESC** | 返回主菜单 |

### 导航快捷键

| 快捷键 | 功能 |
|--------|------|
| **↑/k** | 上移光标 |
| **↓/j** | 下移光标 |
| **Enter/空格** | 选择/确认 |

### 注册机专用快捷键

| 快捷键 | 功能 |
|--------|------|
| **←/h** | 切换到上一个邮箱源 |
| **→/l** | 切换到下一个邮箱源 |
| **1** | 选择 Outlook（焦点不在输入框时） |
| **2** | 选择 CloudMail（焦点不在输入框时） |
| **3** | 选择 HttpAPI（焦点不在输入框时） |
| **Tab** | 切换输入框焦点 |
| **Shift+Tab** | 反向切换输入框焦点 |

### 文本编辑快捷键

| 快捷键 | 功能 |
|--------|------|
| **数字/字母** | 输入字符 |
| **Backspace** | 删除前一个字符 |
| **Delete** | 删除后一个字符 |
| **Ctrl+U** | 清空输入框 |
| **Home/Ctrl+A** | 移到行首 |
| **End/Ctrl+E** | 移到行尾 |

## 💡 使用技巧

### 1. 快速配置

**使用默认值**
```
直接按 Tab 跳过输入框
使用默认值: 数量=10, 并发=3, 延时=5
```

**批量修改**
```
Tab 到输入框
Ctrl+U 清空
输入新值
Tab 到下一个
```

### 2. 邮箱源选择

**快速切换**
```
使用 ← → 键比数字键更直观
支持循环切换，不会"卡住"
```

**记住顺序**
```
1. Outlook   - 最左边
2. CloudMail - 中间（默认）
3. HttpAPI   - 最右边
```

### 3. 任务控制

**优雅停止**
```
不要用 Ctrl+C 强制退出
Tab 到 [停止任务]，按 Enter
等待当前任务完成后停止
```

**查看日志**
```
日志自动滚动显示最近 10 条
使用不同的图标快速识别状态
```

### 4. 错误处理

**常见问题**

问题: 输入框无法输入
```
检查: 焦点是否在输入框上
解决: 按 Tab 切换到正确的输入框
```

问题: 数字键无法切换邮箱源
```
原因: 焦点在输入框上，数字会输入到文本框
解决: 按 Tab 移开焦点，或使用 ← → 键
```

问题: 任务无法启动
```
检查:
1. 邮箱源配置是否正确
2. 参数是否合理
3. 查看日志错误信息
```

## 📊 性能建议

### 推荐配置

**测试阶段**
```
数量: 1-2
并发: 1
延时: 0
用途: 快速验证配置
```

**小规模注册**
```
数量: 5-10
并发: 2-3
延时: 5
用途: 日常使用，稳定优先
```

**大规模注册**
```
数量: 50+
并发: 5-10
延时: 10-15
用途: 批量任务，避免风控
```

### 避免风控

**关键点**
1. **延时不要为 0**: 建议至少 5 秒
2. **并发不要过高**: 建议不超过 10
3. **使用代理池**: 在设置中配置
4. **使用域名池**: CloudMail 时配置多个域名
5. **观察日志**: 频繁失败时增加延时

## 🔧 故障排查

### 问题诊断

**界面异常**
```
症状: 显示混乱或卡住
原因: 终端窗口太小
解决: 调整终端窗口大小（建议 80x24 以上）
```

**按键无响应**
```
症状: 按键没有反应
检查:
1. 终端是否有输入焦点
2. 是否在正确的界面
3. 尝试按 ESC 返回主菜单
```

**任务失败**
```
症状: 所有任务都失败
检查:
1. 邮箱源配置（Outlook 需要先添加账号）
2. 网络连接
3. 代理设置
4. 查看详细日志
```

### 日志分析

**成功案例**
```
21:27:18 ✓ user1@example.com - 成功
21:27:23 ✓ user2@example.com - 成功
```
说明: 配置正确，运行正常

**失败案例**
```
21:27:28 ✗ user3@example.com - 邮箱已注册
```
原因: 邮箱已被使用（正常情况）

```
21:28:15 ✗ user4@example.com - 注册被拦截
```
原因: 触发风控，需要增加延时或更换代理

```
21:28:30 ✗ user5@example.com - 网络连接失败
```
原因: 网络问题，检查网络或代理

## 📚 相关文档

- [TUI 版本总结](TUI_VERSION_SUMMARY.md)
- [项目最终总结](PROJECT_FINAL_SUMMARY.md)
- [并发延时配置](docs/CONCURRENT_DELAY.md)
- [SSO 快速开始](docs/SSO_QUICKSTART.md)

## 🎯 高级功能（开发中）

### 号池管理
```
功能: 查看和管理已注册的账号
状态: 框架已准备
计划: 下一版本
```

### 网关控制
```
功能: 启动/停止本地 Kiro 网关
状态: 框架已准备
计划: 下一版本
```

### 设置界面
```
功能: 配置代理池、域名池等
状态: 框架已准备
计划: 下一版本
```

## 💬 反馈和支持

遇到问题或有建议？

- **GitHub Issues**: https://github.com/NevinXuHui/kiro-client/issues
- **Pull Requests**: https://github.com/NevinXuHui/kiro-client/pulls

---

**更新日期**: 2026-08-24
**版本**: TUI v1.0
**分支**: tui-version

🎉 **享受使用 Kiro Client TUI！**
