# Kiro Client 功能总结

## ✅ 并发注册延时配置（已完成）

### 前端 UI

**位置**: 注册机页面

```
┌─────────────────────────────────┐
│ 注册配置                        │
├─────────────────────────────────┤
│ 注册数量: [10]                  │
│ 并发数:   [3]                   │
│ 延迟(秒): [5]  ← 这里！         │
└─────────────────────────────────┘
```

**代码位置**:
- `frontend/index.html` 第 269-270 行
- `frontend/js/app.js` 第 293 行（读取）
- `frontend/js/app.js` 第 400 行（保存/恢复）

**特性**:
- 默认值: 3 秒
- 支持小数（0.5 秒精度）
- 最小值: 0（不延时）

### 后端逻辑

**位置**: `internal/task/coordinator.go`

**并发模式**（第 636-638 行）:
```go
// 在启动任务前延时
if req.Delay > 0 && i > 0 {
    time.Sleep(time.Duration(req.Delay) * time.Second)
}
```

**串行模式**（第 655-657 行）:
```go
// 在任务完成后延时
if req.Delay > 0 && i < req.Count-1 {
    time.Sleep(time.Duration(req.Delay) * time.Second)
}
```

### 工作流程

#### 并发模式示例
```
配置: 10个账号, 并发3, 延时5秒

时间轴:
0s   → 启动账号1 ████████████████ (15秒)
5s   → 启动账号2    ████████████████ (15秒)
10s  → 启动账号3       ████████████████ (15秒)
15s  → 启动账号4 (等待账号1完成)
...

日志输出:
[Kiro] 启动并发任务: 10 个任务，并发数 3，延时 5 秒
[Kiro][1/10] 开始注册...
(5秒后)
[Kiro][2/10] 开始注册...
(5秒后)
[Kiro][3/10] 开始注册...
```

#### 串行模式示例
```
配置: 10个账号, 并发1, 延时5秒

时间轴:
账号1 注册 ████████████████ (15秒)
延时 ░░░░░ (5秒)
账号2 注册 ████████████████ (15秒)
延时 ░░░░░ (5秒)
...

日志输出:
[Kiro] 启动串行任务: 10 个任务，延时 5 秒
[Kiro][1/10] 开始注册...
[Kiro][1/10] 注册完成
等待 5 秒...
[Kiro][2/10] 开始注册...
```

### 推荐配置

| 场景 | 数量 | 并发 | 延时 | 说明 |
|------|------|------|------|------|
| 测试 | 1-2 | 1 | 0 | 快速测试 |
| 少量注册 | 3-5 | 1-2 | 3-5 | 稳定优先 |
| 批量注册 | 10-20 | 3-5 | 5-10 | 平衡速度和风控 |
| 大量注册 | 50+ | 5-10 | 10-15 | 避免触发风控 |

### 使用说明

1. **打开注册机页面**
   - 点击侧边栏"注册机"

2. **配置参数**
   - 注册数量: 想要注册的账号数量
   - 并发数: 同时运行的注册任务数
   - 延迟(秒): 每个账号启动间隔

3. **开始注册**
   - 点击"开始注册"按钮
   - 观察日志输出

4. **查看效果**
   - 日志会显示延时配置
   - 任务按延时间隔启动
   - 成功率更高，风控更少

### 优势

1. **降低风控概率**
   - 避免瞬间大量请求
   - 平滑流量曲线
   - 更像人工操作

2. **可控的并发**
   - 既保持并发效率
   - 又控制启动频率
   - 灵活调整策略

3. **完整的日志**
   - 显示延时配置
   - 便于调试和监控
   - 了解任务执行情况

### 注意事项

1. **延时为 0**
   - 所有任务瞬间启动
   - 可能触发风控
   - 仅测试时使用

2. **延时过大**
   - 总耗时增加
   - 效率降低
   - 根据实际情况调整

3. **配合代理池**
   - 使用代理池可以适当减少延时
   - 但仍建议保持 3-5 秒延时
   - 避免瞬时冲击

4. **观察日志**
   - 如果频繁被拦截
   - 增加延时或减少并发
   - 调整到合适的配置

## 相关文档

- [并发延时配置详细说明](docs/CONCURRENT_DELAY.md)
- [任务协调器源码](internal/task/coordinator.go)
- [前端注册页面](frontend/index.html)

## Git 提交历史

```
3e3775a docs: 添加并发延时配置文档
17a8000 feat(task): 并发注册支持账号间延时配置
```

## 总结

✅ **功能已完整实现**

- UI 层: 有延时输入框 ✓
- 前端: 正确传递参数 ✓
- 后端: 实现延时逻辑 ✓
- 日志: 显示延时配置 ✓
- 文档: 完整的说明 ✓

用户可以直接在 GUI 界面中配置延时参数，
程序会自动按配置执行并发注册！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
